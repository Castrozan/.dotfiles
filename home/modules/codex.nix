# codex - OpenAI's CLI coding agent (module entrypoint)
#
# Keep this file as a stable import target. The implementation lives in
# ./codex/* to match the Claude module layout pattern.
{ ... }:
{
  imports = [
    ./codex
  ];
}

