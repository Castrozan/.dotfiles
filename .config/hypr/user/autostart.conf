# Cursor config
exec-once = hyprctl setcursor Bibata-Modern-Classic 24

# Initialize theme system (restore last theme or set catppuccin as default)
# exec-once = omarchy-theme-set "$(cat ~/.config/omarchy/current/theme.name 2>/dev/null || echo catppuccin)"

# Default to workspace 11 (center of 3x7 grid: row 2, col 4)
exec-once = hyprctl dispatch workspace 11

# Start a terminal
exec-once = wezterm
