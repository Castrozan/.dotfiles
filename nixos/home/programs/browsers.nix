{
  pkgs,
  config,
  username,
  ...
}: {
  # TODO: fix this. Uncomment this and run it. It breaks home manager
  # programs = {
  # chromium = {
  #   enable = true;
  #   commandLineArgs = ["--enable-features=TouchpadOverscrollHistoryNavigation"];
  #   extensions = [
  #     # {id = "";}  // extension id, query from chrome web store
  #   ];
  # };

  # firefox = {
  #   enable = true;
  #   profiles.${username} = {};
  # };
  # };
}
