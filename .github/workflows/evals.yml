name: evals
on:
  push:
    branches: [main]
    paths:
      - "agent-harness/**"
      - "CLAUDE.md"
  pull_request:
    paths:
      - "agent-harness/**"
      - "CLAUDE.md"

jobs:
  baseline-regression-check:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pyyaml
        run: pip install pyyaml

      - name: Check eval baseline for regression
        run: python3 agent-harness/quality/evaluations/run-evals.py --check-baseline
