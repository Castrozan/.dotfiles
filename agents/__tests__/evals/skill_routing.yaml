shared_system_prompt: &shared_system_prompt |
  You are a skill router. Given a user request, respond with the name of the single most appropriate skill to handle it. Respond with ONLY the skill name, nothing else.
  Available skills:

  all-skills - This skill compiles all skills not curatedly injected into interactive agent sessions for agent reachability and context management. The skills here are for: arr-stack, avatar, b3-portal, daily-report, goal-prompt, google-chat, jira, morning-briefing, notify, obsidian, passwords, quickshell, ril, todo, twitter, worktrees, youtube

  architecture - Boundaries, dependency direction, system design, UML and deployment shape. Use when choosing a structure, adding a module or service, moving a boundary, or reviewing a design.

  browser - Interact with a live webpage inside a browser window: fill forms, click buttons, navigate authenticated apps, automate Electron apps, test web UI, capture browser-window screenshots for visual verification of web pages. Do NOT use for fetching or reading web content programmatically (use curl or MCP fetch). Do NOT use for desktop screenshots, system media/audio control, MPRIS players, or non-browser GUI; those belong to the desktop skill.

  claude-harness - How the Claude Code and Codex harnesses actually behave - settings and config resolution, transcript and project-dir layout, hooks, MCP and connector gating, subagent and model routing, token budgets, and the tool quirks that make a command land somewhere you did not intend. Read when debugging the harness itself rather than the code it is editing.

  clawde - Operate the clawde autonomous agent fleet - declare or change an agent, make a change actually reach the running agents, and debug one that is dead, unresumed, wedged, silent, or spending on the wrong model. Covers the steward loop. Read before touching any agent, supervisor, or heartbeat behavior.

  code-craft - Clean-code standards for writing and judging implementation: DRY, SOLID, coupling and cohesion, function shape, test-first, and when not to abstract.

  code-review - Review a diff for defects and craft, from token and name through function shape to system design, and request changes. Use when judging code, yours or a delegate's.

  deep-work - Persist task context to disk so big or compaction-prone work survives across sessions. Use for work over ~5 steps, spanning sessions, or when explicitly asked to preserve context; not for quick fixes.

  deliver - Drive a large software goal end-to-end - investigate context, design the build process, author a goal prompt, then execute with workflows and subagents until it works live. For big objectives needing a process, not one-off tasks.

  desktop - Desktop automation and media control: type text, send key combos, click/move/scroll/drag mouse, capture screenshots, read/write clipboard, music and audio playback (pause, play, next, previous, volume). Cross-platform for screenshots, clipboard, and media control (Linux/Wayland + macOS); keyboard and mouse remain Linux/Wayland-only. Use for any non-browser GUI interaction, any request to pause/play music, adjust volume, or control local media. For YouTube CLI, see the youtube skill.

  docs - Documentation standards for when a doc earns its place, what never to write, evergreen phrasing, and policy shape. Use when writing or judging a README, doc, or policy; for authoring AI instruction files read instructions.

  exit - End the current Claude Code session cleanly. Use when all work is complete and committed and the session should terminate.

  explore - Search a codebase in three widening-then-narrowing cycles, using text, file, history and context search, and return the conclusion rather than the files.

  git - Git operations: creating high-quality conventional commits with meaningful messages and disciplined staging, and searching commit history efficiently via cached log dumps. Use when committing, staging changes, writing commit messages, investigating past decisions, finding when code changed, or tracing feature evolution through the repo's history.

  herdr - Drive herdr, the terminal workspace manager, and orchestrate Claude Code agents in it: spawn, prompt, read, wait on, or take over a background Claude. Also scripts herdr workspaces, tabs, and panes.

  housekeeping - Recurring housekeeping sweep of the dotfiles tree for standing rot nothing else catches: stale markers, dead code, orphaned files, instruction drift, convention debt, chronic infra traps, test gaps. Triggers the dotfiles-housekeeping workflow.

  humanize - The single home for how text a human reads is worded: plain-language discipline, AI-writing tells to strip, per-channel phrasing and length (chat, commit, MR, ticket, doc, page), and the wording rules a hook blocks on. Load before drafting any human-facing text.

  instructions - Authoring AI instruction surfaces - SKILL.md, agent definitions, CLAUDE.md policies, and subagent briefs. Use when writing or editing any file that instructs an AI.

  nix - Nix everything: language expertise (lazy eval, derivations, overlays, module system, flakes, agenix, cachix), applying config via rebuild, devenv project shells, THIS dotfiles repo conventions (module layout, isNixOS, script packaging, secrets, channels), and Docker/Podman container management via the docker-manager script. Use for any .nix edit, rebuild, devenv shell, container lifecycle, or "where does this belong in the dotfiles" question.

  quality-assurance - Final QA method: verify finished work against the user's stated goal, widen the scope past the tests the plan named, and hunt side effects in untouched behavior.

  research - Current-information research and synthesis for tools, vendors, standards, or decisions. Use when the user asks to research, compare, evaluate, verify, or find the latest external information; not for local repo search.

  research-digest - On-demand research digest for any topic passed at invoke time; fans out across GitHub, arXiv, HN, Reddit, Lobste.rs and X, then dedups and ranks into a themed pulse. Use for "research digest", "catch me up on <topic>", or a curated AI/dev community pulse.

  restart - Restart the current Claude Code session in place, resuming the same session with an optional continuation prompt. Use to reload after config changes or resume work without waiting for input.

  review - Compliance auditing, instruction-file audits, and skill routing repair. Use when checking conventions, auditing a skill or CLAUDE.md edit, or fixing a skill that never fires; for judging code read code-review, for writing docs read docs, for how prose is worded read humanize.

  test - Testing methodology and verification workflow. Use when implementing, fixing, or modifying any code. Defines the test-first, test-after, and pre-delivery verification protocol.

  workspace - Switch the Bash working directory to another repo or directory for the session. Use when work must run commands outside the primary repo.

tests:
  - name: routing_to_desktop_keyboard_typing
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Type 'hello world' into the terminal window"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_to_desktop_screenshot_capture
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Take a screenshot of my current screen"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_to_desktop_clipboard_access
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Copy the selected text to clipboard and paste it into another window"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_to_desktop_mouse_click
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Click on the close button at position 1900,10"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_to_media_volume_adjustment
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Turn the volume up to 80%"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_to_media_youtube_search
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Search YouTube for NixOS tutorials and add the best one to my playlist"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_media_skip_track
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Skip to the next song"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_to_personal_google_chat_message
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Send a message to the team on Google Chat saying the deploy is done"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_twitter_post
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Post a tweet about the new release on my X account"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_vtuber_avatar
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Make the VTuber avatar smile and start lip syncing"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_twitter_scrape
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Scrape the latest tweets from @nixos_org"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_deep_work
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "This is a big task, set up a deep work workspace to track progress"
    model: sonnet
    assertions:
      output_equals:
        - "deep-work"

  - name: routing_to_worktrees
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Create a git worktree so I can work on a parallel branch"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_tmux_control
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Start a background process in a new herdr pane"
    model: sonnet
    assertions:
      output_equals:
        - "herdr"

  - name: routing_to_restart
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Restart the current session and preserve context"
    model: sonnet
    assertions:
      output_equals:
        - "restart"

  - name: routing_to_browser_web_scraping
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Open the AWS console and check the EC2 instances page"
    model: sonnet
    assertions:
      output_equals:
        - "browser"

  - name: routing_to_browser_form_filling
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Fill out the login form on https://example.com with my credentials"
    model: sonnet
    assertions:
      output_equals:
        - "browser"

  - name: routing_to_browser_electron_app
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Automate the VS Code Electron app to open a file and run a command"
    model: sonnet
    assertions:
      output_equals:
        - "browser"

  - name: routing_to_browser_interactive_scrape
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Open https://example.com/pricing in a browser, click the 'Load More' button, then extract the pricing table"
    model: sonnet
    assertions:
      output_equals:
        - "browser"

  - name: routing_to_claude_spawn_agent
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Spawn a parallel agent to refactor the utils module while I work on tests"
    model: sonnet
    assertions:
      output_equals:
        - "herdr"

  - name: routing_to_commit_staging
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Stage the changed files and create a commit with a good message"
    model: sonnet
    assertions:
      output_equals:
        - "git"

  - name: routing_to_home_assistant_lights
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Turn on the living room lights and set brightness to 50%"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_home_assistant_ac_control
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Set the air conditioner to 22 degrees and fan speed to high"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_ril_capture_backlog
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Go through the links I saved this week, one by one, and tell me what to do with each"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_dotfiles_module_addition
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Add a new home-manager module for configuring zathura PDF viewer"
    model: sonnet
    assertions:
      output_equals:
        - "nix"

  - name: routing_to_nix_expert_expression
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Help me write a Nix overlay that patches the source of a package"
    model: sonnet
    assertions:
      output_equals:
        - "nix"

  - name: routing_to_rebuild_apply_changes
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Rebuild the system to apply the flake changes"
    model: sonnet
    assertions:
      output_equals:
        - "nix"

  - name: routing_to_test_verification
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Run the test suite to verify the changes pass"
    model: sonnet
    assertions:
      output_equals:
        - "test"

  - name: routing_to_review_skill_creation
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Create a new skill for managing Docker containers with good prompts"
    model: sonnet
    assertions:
      output_equals:
        - "review"

  - name: routing_to_docs_readme_standards
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "I want to add a README to this module. What belongs in it and what should I leave out?"
    model: sonnet
    assertions:
      output_equals:
        - "docs"

  - name: routing_to_review_skill_routing_failure
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "The model keeps using WebFetch instead of the twitter skill when I paste Twitter URLs. How do I fix the skill routing?"
    model: sonnet
    assertions:
      output_equals:
        - "review"

  - name: routing_to_notify_task_completion
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Notify me when the long refactoring task is complete"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_quickshell_bar_module
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Add a new battery widget to the Quickshell bar"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_research_tool_comparison
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Research the latest Wayland compositors and compare their features"
    model: sonnet
    assertions:
      output_equals:
        - "research"

  - name: routing_to_review_compress_output
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "That answer was way too long, give me the short version"
    model: sonnet
    assertions:
      output_equals:
        - "review"

  - name: routing_to_devenv_shell_setup
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Set up a devenv shell with PostgreSQL and Python 3.12 for this project"
    model: sonnet
    assertions:
      output_equals:
        - "nix"

  - name: routing_to_phone_status_battery
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Check my phone battery level and charging status"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_ponto_time_tracking
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Fill my ponto entries for this week on Senior"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_ambiguous_browser_vs_desktop_screenshot
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Take a screenshot of my desktop showing all open windows"
    model: sonnet
    assertions:
      output_equals:
        - "desktop"

  - name: routing_ambiguous_research_vs_browser
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "What are the best practices for Nix flake design in 2026?"
    model: sonnet
    assertions:
      output_equals:
        - "research"

  - name: routing_ambiguous_commit_vs_worktrees
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "I want to commit all my staged changes with a conventional commit message"
    model: sonnet
    assertions:
      output_equals:
        - "git"

  - name: routing_to_code_review_for_an_unbiased_reviewer_pass
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Spawn an unbiased reviewer agent to check my implementation for bugs and convention violations"
    model: sonnet
    assertions:
      output_equals:
        - "code-review"

  - name: routing_to_git_history_search_commits
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Search git history for all commits related to browser automation"
    model: sonnet
    assertions:
      output_equals:
        - "git"

  - name: routing_to_git_history_find_when_changed
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "When was the authentication module first added to this repo?"
    model: sonnet
    assertions:
      output_equals:
        - "git"

  - name: routing_to_git_history_trace_evolution
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Show me how the browser skill evolved over time in this project"
    model: sonnet
    assertions:
      output_equals:
        - "git"

  - name: routing_to_git_history_investigate_past
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "I need to understand why we switched from playwright to chrome devtools, check the git history"
    model: sonnet
    assertions:
      output_equals:
        - "git"

  - name: routing_ambiguous_git_history_vs_research
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Research the latest trends in version control systems"
    model: sonnet
    assertions:
      output_equals:
        - "research"

  - name: routing_to_ril_inbox_processing
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Process my saved articles from the ReadItLater inbox"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_tweet_media_fetch
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Fetch the image attached to this tweet by @user"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_ambiguous_browser_vs_personal_tweet_scraping
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "I need to read what a tweet says and see its attached image"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_bare_xcom_url
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "https://x.com/neogoose_btw/status/2040598081763471417"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_bare_twitter_url
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "https://twitter.com/elikiaponde/status/1234567890"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_xcom_url_with_context
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "https://x.com/schizoidcock/status/2040595546411266214 this looks interesting, is it any good?"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_personal_xcom_url_what_does_it_say
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "what does this tweet say? https://x.com/user/status/1234567890123"
    model: sonnet
    assertions:
      output_equals:
        - "all-skills"

  - name: routing_to_code_review_for_a_diff_before_merge
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Here is the diff on the auth module. Check it for bugs and bad naming before I merge it."
    model: sonnet
    assertions:
      output_equals:
        - "code-review"

  - name: routing_to_review_for_an_instruction_file_audit
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "I rewrote a SKILL.md and a CLAUDE.md section. Audit them against our instruction authoring standards."
    model: sonnet
    assertions:
      output_equals:
        - "review"

  - name: routing_to_architecture_for_a_boundary_decision
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Should the payment retry logic live in its own service or stay inside the order module?"
    model: sonnet
    assertions:
      output_equals:
        - "architecture"

  - name: routing_to_code_craft_for_a_function_cleanup
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "This function does three things and takes a boolean flag argument. Clean it up."
    model: sonnet
    assertions:
      output_equals:
        - "code-craft"

  - name: routing_to_quality_assurance_for_the_final_pass
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "The feature is implemented and its unit tests pass. Do a final pass to confirm what I actually asked for works and nothing else broke."
    model: sonnet
    assertions:
      output_equals:
        - "quality-assurance"

  - name: routing_to_explore_for_a_repo_sweep
    no_tools: true
    system_prompt: *shared_system_prompt
    prompt: "Where in this repo is the retry timeout configured? Sweep the whole tree and just tell me the answer."
    model: sonnet
    assertions:
      output_equals:
        - "explore"
