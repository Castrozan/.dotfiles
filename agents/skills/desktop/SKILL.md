---
name: desktop
description: Desktop automation and media — screenshots, clipboard, keyboard input, mouse control, media playback via MPRIS/playerctl (play, pause, volume, next), and YouTube search and playlist management via youtube-cli. Wayland-native. Use for any non-browser GUI interaction or for local/YouTube media operations.
---

Desktop interaction skill. Each capability has its own doc and script.

For screenshots (full, region, active window), read `screenshot.md`.
For clipboard read/write/watch, read `clipboard.md`.
For keyboard input (type text, send key combos), read `keyboard.md`.
For mouse control (click, move, scroll, drag), read `mouse.md`.
For media playback control (play, pause, volume, MPRIS players), read `media-control.md`.
For YouTube operations (search, playlists, add/remove videos via youtube-cli), read `youtube.md`.
