name: skill_discovery_natural_worktrees_parallel_branch
description: Natural-prompt variant. User asks for a parallel branch without touching the current tree, with no hint about worktrees or the worktrees skill. Agent should still route to the worktrees skill.

setup:
  git_init: true
  files:
    - path: README.md
      content: "demo repo"

prompt: |
  I need a parallel git branch without touching my current tree.

timeout: 180

assertions:
  autonomous_skill_invocation:
    - worktrees
  wrong_skill_not_invoked:
    - git
    - nix
    - personal
