---
name: distribution-probe
description: Verify that a distributed plugin can discover its skill and call its MCP tool.
---

### Probe

Call `distribution_echo` with the requested token. Return the tool response unchanged. Read the
[expected behavior](references/expected.md) when checking the result.
