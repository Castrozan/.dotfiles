# git-crypt: DO NOT encrypt private-config/ — Nix flakes copy files
# from git object store without running smudge filters, so encrypted
# files cause evaluation failures. Use agenix for runtime secrets instead.
